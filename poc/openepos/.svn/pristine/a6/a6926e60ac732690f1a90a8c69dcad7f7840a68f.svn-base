package keso.driver.serialport;

public class RingBuffer {
	public byte[] buf;
	public int head;
	public int tail;
	public int count;
	
	public RingBuffer(int size) {
		buf = new byte[size];
		head = tail = count = 0;
	}
}
