#include "threadmapper.h"
#include "utility/malloc.h"

//__USING_SYS




//template<class T, int _max>
//ThreadMapper<T, _max> ::ThreadMapper() : _i(0){ }

/*
template<class T, int _max>
ThreadMapper<T, _max> * ThreadMapper<T, _max>::instance(){
    if(!_instance)
        _instance = new ThreadMapper();
    return _instance;
}
*/

/*
template<class T, int _max>
void ThreadMapper<T, _max>::insertElement(T  th, unsigned int i)
{
    _o[_i] = th;
    _e[_i] = new typename Simple_Hash<T, _max, unsigned int>::Element(&_o[_i], i ) ;
    _hash.insert(_e[_i]);
    _i++;
}


*/
